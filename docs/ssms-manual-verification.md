# SSMS 수동 확인 — 남은 항목

2026-09-18 기준(0.9.0). CI가 검증하지 못하는 영역(WPF 렌더링, VS 패키지 로딩, `.vsct` 메뉴, 옵션
페이지 등록, SSMS 통합, 실제 DB 연결, 실제 AI 프로바이더 호출)에서 **SSMS 21 실기 확인 기록이 없는**
항목을 모았다.

계획서의 체크박스는 근거로 쓰지 않았다 — 실기로 확인한 AI 기능 계획서도 전부 비어 있을 만큼
채우는 습관이 들쭉날쭉하다. 대신 `team-rollout-backlog.md`, 커밋 메시지,
`superpowers/HANDOFF-2026-08-31.md`에 남은 실기 기록과 대조했다. **기록이 없다는 것이지 안 했다는
뜻은 아니다** — 이미 해 본 항목은 날짜와 함께 체크한다.

기능(버전)별이 아니라 **한 번 앉아서 이어 밟을 수 있는 준비 상태**별로 묶었다. 같은 클론·같은
화면에서 끝나는 것끼리 모여 있다. 옛 버전 항목이 이후 버전에 흡수되었거나 이미 밟힌 경우는 빼고,
뺀 이유를 맨 아래에 남겼다.

## 적는 법

- 확인했으면 `[x]`로 바꾸고 줄 끝에 `— YYYY-MM-DD` 를 붙인다.
- 실패했으면 체크하지 말고 바로 아래에 `> 실패: 증상` 을 적는다.
- 해 보지 않고 넘길 항목은 `> 보류: 사유` 를 적는다.

## 권하는 순서

A·E·D-1·B-1·B-2는 끝났다(2026-09-17). 남은 것:

1. **G. 도구 줄** — 0.9.0이 화면을 통째로 바꿨다. 다른 절을 밟다 눌러 볼 버튼 자리가 달라졌으므로
   먼저 한 바퀴 돈다. 개발 클론 하나와 배포 클론 하나면 된다
2. **B-3·B-4 → C** — 같은 개발 클론에서 이어진다
3. **D-2·D-3** — 지금의 배포 클론(`Northwind_Deploy`)으로 된다
4. **F. 병합** — 원격에 `master`와 운영 클론이 더 있어야 해서 준비가 가장 많다. D를 끝낸 뒤 한다.
   G-4의 마지막 항목(0.8.0 회귀)이 이 절을 그대로 한 번 더 밟는 것이라 붙여서 하면 한 번으로 끝난다

---

## A. 개발 클론 — 브랜치 조작 (0.6.0)

준비: 개발 용도(`Write`)로 연결한 클론, 원격 접근(SSH).
출처: `superpowers/plans/2026-09-10-dbvc-branch-operations.md` Task 8 Step 5, 커밋 `10d92e6`

- [x] `develop`에서 **새 브랜치** → 미커밋 추출물이 그대로 남아 있고 브랜치 표시가 바뀐다 — 2026-09-17
- [x] 커밋 → **Push** → 확인 대화상자가 뜨고, 동의하면 원격에 브랜치가 생긴다 — 2026-09-17
- [x] 같은 브랜치에서 **두 번째 Push는 묻지 않는다** — 2026-09-17
- [x] 추출물이 남은 채 **브랜치 전환** → 거부되고 파일 이름이 보인다 — 2026-09-17
- [x] 한 SSMS 세션에서 브랜치 대화상자를 **두 번 이상** 열어도 예외가 나지 않는다 — 2026-09-17
      (닫힌 WPF 창은 다시 띄울 수 없어 두 번째부터 죽던 자리다 — 단위 테스트가 닿지 않는다)

## B. 개발 클론 — 커밋 흐름

준비: 개발 용도 클론, 객체를 고칠 수 있는 DB. **작성자 항목은 전역 `git config`가 없는 계정**이
필요하고, **정리 실패 항목은 `sysadmin`이 아닌 계정**이 필요하다.

### B-1. 커밋 작성자 신원 (0.5.17)

출처: `setup-checklist.md` "0.5.17 — 커밋 작성자 신원"

- [x] 전역 `git config`가 없는 계정에서 저장소를 연결하면 작성자 배너가 뜬다 — 2026-09-17
- [x] **작성자 설정...**을 눌러 입력하면 배너가 사라진다 — 2026-09-17
- [x] 배너를 무시하고 커밋을 누르면 막히고 입력 창이 뜬다. 입력하면 커밋이 이어진다 — 2026-09-17
- [x] 그 커밋의 작성자가 `git log`와 GitLab에서 본인으로 보인다 — 2026-09-17
- [x] 설정 후 저장소의 `.git/config`에만 값이 들어갔고 `git config --global`은 그대로다 — 2026-09-17
- [x] 전역 `git config`가 이미 있는 계정에서는 배너가 뜨지 않는다 — 2026-09-17

### B-2. 되돌리기 (0.5.18 · 0.5.19)

0.5.19의 집계 오류가 실기에서 나왔으므로 한 번은 밟았다. 전 항목을 봤다는 기록은 없다.
출처: `setup-checklist.md` "되돌리기 수동 확인 (0.5.18)"

- [x] 수정 항목 **되돌리기** → 확인 문구에 "되돌릴 파일 1개" → 파일이 저장소 내용으로 돌아온다 — 2026-09-17
- [x] **그 항목은 목록에 남고**, 새로고침하면 다시 더러워진다 (설계대로다) — 2026-09-17
- [x] `추가` 항목 **되돌리기** → 확인 문구에 파일 이름과 "복구되지 않습니다" → 파일이 사라진다 — 2026-09-17
- [x] **다른 프로세스가 배타적으로 잠근** `.sql`을 포함해 둘 이상 되돌리기 → **실패 상자**가 뜨고
      나머지 파일은 처리된다. 잠긴 파일이 성공 수에 섞이지 않는다 (0.5.19에서 고친 집계) — 2026-09-17

  > SSMS·메모장으로 파일을 열어 둔 채로는 실패가 뜨지 않았고 파일도 정상으로 되돌아왔다 — 두 편집기
  > 모두 잠금을 쥐지 않아 전제 조건이 성립하지 않은 것이다. PowerShell로 배타적으로 잠가 다시 밟았다.
  > 원래 절차("편집기로 열어 둔 채")는 `setup-checklist.md`에서 함께 고쳤다.
  >
  > ```powershell
  > $h = [IO.File]::Open('<저장소>\dbo\...\<파일>.sql', 'Open', 'ReadWrite', 'None')   # 끝나면 $h.Dispose()
  > ```

### B-3. 변경 로그 정리 실패 안내 (0.5.14)

배너와 **추적기 업데이트**는 2026-09-09 v5 → v6 업그레이드에서 밟았다. 남은 것은 권한과 실패 경로다.
출처: `setup-checklist.md` "0.5.14 — 변경 로그 정리 실패 안내" (DENY/REVOKE 쿼리가 거기 있다)

- [ ] 업데이트 뒤 "일상 사용에 필요한 권한" 조회 쿼리가 `SELECT`·`UPDATE` 두 줄을 낸다
- [ ] `DENY UPDATE`를 건 뒤 `sysadmin`이 아닌 계정으로 커밋 → 제목이 **"DBVC 커밋 — 변경 로그 정리"** 인 상자가 뜬다(제목에 "실패"가 없다)
- [ ] 본문이 커밋 성공 / 항목이 다시 나타남 / `db_owner`가 업데이트하면 됨, 세 가지를 모두 말한다
- [ ] 상자를 닫으면 새로고침이 돌고 그 항목이 목록에 남아 있다
- [ ] `REVOKE` + `GRANT`로 되돌린 뒤 평범한 커밋에는 **아무 상자도 뜨지 않는다**

### B-4. 추출물의 한국어

- [ ] 한국어 주석·확장 속성이 있는 객체를 고쳐 추출한 `.sql`을 SSMS로 열면 깨지지 않는다

## C. 개발 클론 — 이력 탭

준비: 커밋이 여러 개 쌓인 개발 클론. 병합 커밋 만드는 절차는 `setup-checklist.md` 0.5.12·0.5.13 절 C에 있다.

### C-1. 분할선과 병합 안내 (0.5.12 · 0.5.13)

이력 뷰 통합(09-02) 계획의 "2단/3단", "전체 이력으로", "`병합` 종류 열" 항목은 이후 이 절이
더 자세한 형태로 대체했다. **항목이 많아 여기에 옮기지 않는다** — 원문에서 체크한다.

- [ ] `setup-checklist.md` "0.5.12 · 0.5.13" **A. 분할선 — 전체 이력 모드** 8항목
- [ ] 같은 절 **B. 분할선 — 필터 모드** 6항목
- [ ] 같은 절 **C. 병합 안내 — 위치** 5항목
- [ ] 같은 절 **D. 병합 안내 — 필터 모드** 1항목
- [ ] 같은 절 **E. 잘림 안내** — 객체 500개 넘는 DB가 있을 때만. 없으면 보류

### C-2. 개체 탐색기 진입과 목록

출처: `superpowers/plans/2026-09-02-dbvc-history-view-consolidation.md` "마무리"

- [ ] 다른 서버·DB의 객체를 우클릭 → **DBVC: 이력 보기** → 두 대상을 모두 알리는 안내가 뜬다
- [ ] DBVC를 연결하지 않은 채 우클릭 → 연결하라는 안내가 뜬다
- [ ] 객체 이름을 바꾸고 커밋하면 그 커밋의 파일 목록에 `삭제`/`추가` 두 줄이 나온다
- [ ] 커밋 목록을 방향키로 빠르게 훑어도 SSMS가 멈추지 않는다

## D. 배포·감사 클론 (0.5.0)

준비: `setup-checklist.md` 8단계. 계획서가 "의도적으로 미완료"로 남겼고, HANDOFF-2026-08-31에도
"D 그룹: 시작 못 함"으로 남아 있다. 09-09에 배포 클론을 한 번 열었지만 `.gitattributes`만 봤다.

> 로컬 `Northwind_Deploy_Clone`은 2026-09-17에 원격 `main`과 같고 UTF-8임을 확인했다. 그 뒤에 원격이
> 움직였으면 차이 검사 전에 Pull한다.

### D-1. 화면 잠금

- [x] 초기화 오버레이가 **뜨지 않는다** — 2026-09-17
- [x] 커밋·Push·새로고침·초기화 버튼이 **잠겨 있다** — 2026-09-17
- [x] 변경 목록 패널이 없다 — 되돌리기·무시 버튼도 보이지 않는다 — 2026-09-17
- [x] 새 브랜치·브랜치 전환 버튼이 비활성이거나 뜨지 않는다 (0.6.0) — 2026-09-17
      (**0.9.0에서 모습이 바뀌었다** — 두 버튼이 윗줄 브랜치 이름 **▾** 메뉴 안으로 들어갔다. G-4에서 다시 본다)
- [x] 차단 오버레이(브랜치 불일치, 미커밋 변경)가 **배포 패널도 덮는다** — 2026-09-17
- [x] 옛 추적기가 남은 DB를 배포 용도로 연결해도 **"변경 추적기가 구버전입니다" 배너가 뜨지 않는다** (0.7.3) — 2026-09-17

> 0.7.3 전에는 이 경우 누를 수 없는 **추적기 업데이트** 버튼과 함께 배너가 떴다(`7cc4dfa`에서 고침).
> 확인 뒤 `Northwind_Deploy`의 옛 추적기(v4)를 지워, 실제 테스트·운영 DB처럼 추적기가 없는 상태에서
> 첫 항목을 봤다.

### D-2. 차이 검사와 배포 스크립트

- [ ] `[차이 검사]` 중 SSMS가 잠기지 않는다 — 개체 탐색기를 클릭해 본다
- [ ] 검사 중 취소 버튼이 실제로 먹는다
- [ ] 목록에서 항목을 고르면 좌우 diff가 뜬다 (브랜치 / 데이터베이스)
- [ ] `[배포 스크립트 저장...]`이 파일을 만들고, 머리말의 제외 사유가 사유별로 나뉘어 있다
- [ ] 저장한 스크립트를 SSMS로 열면 한국어 주석이 깨지지 않는다 (0.5.15에서 고친 BOM)
- [ ] 그 스크립트를 실행한 뒤 `[차이 검사]`를 다시 누르면 그 항목이 사라진다

### D-3. 연결

- [ ] 연결 대화상자에서 배포/감사를 고르고 브랜치를 비우면 **"고정할 브랜치를 입력해야 합니다"** 로 거부된다
- [ ] 원격에서 고정 브랜치를 지정해 받으면(저장소 받기) 받은 직후 차단되지 않는다

## E. AI 커밋 메시지 (0.7.x)

2026-09-16 실기에서 옵션 페이지·키 암호화(`ai-settings.json`에 원문 없음)·생성까지 확인했다
(`team-rollout-backlog.md` 머리말). 남은 것:

- [x] **레지스트리에 평문이 남지 않는다** — VS 설정 저장소에 `AiOptionPage`의 속성(주소·모델·키)이
      기록되지 않았다. `base.SaveSettingsToStorage` 한 줄로 깨지고, 깨져도 빌드·테스트·화면 어디에도
      드러나지 않는다 — 2026-09-17
- [x] **`7a19d26`(명사형 문체) 이후** 사내 서버(`pelly:latest`)가 "기능 추가", "오류 수정" 같은
      명사형으로 답한다 — 2026-09-17
- [x] 주소를 일부러 틀리게 넣고 `연결 테스트` → 한국어 사유가 뜬다 (`982da86`에서 고친 안내) — 2026-09-17

## F. 배포·감사 클론 — 병합 (0.8.0)

다른 세션이 `68ab500`으로 들였다. 출처: `setup-checklist.md` 8단계 "배포·감사 클론 병합 (0.8.0)",
`superpowers/specs/2026-09-17-dbvc-merge-in-deploy-clone-design.md` 4.2.

### F-0. 준비 — 지금 환경으로는 절반만 된다

**환경 브랜치 이름이 코드에 박혀 있다** — `develop`과 **`master`** 다(`EnvironmentBranches`). 검증용
원격(`db-schema-Northwind`)의 운영 브랜치는 `main`이라, 그대로 두면 운영 클론 쪽 판정(경고 A, 테스트
반영 열)이 아예 켜지지 않고 `main`이 병합 후보 목록에 섞여 나온다.

- [ ] 원격에 `master` 브랜치를 만든다 (`main`에서 분기해 Push)
- [ ] **테스트 클론:** 배포 용도, 고정 브랜치 `develop`. 지금의 `Northwind_Deploy` 매핑이 `main`
      고정이므로 `develop`으로 다시 연결한다
- [ ] **운영 클론:** 고정 브랜치 `master`. 매핑이 DB 단위라 **DB가 하나 더 필요하다** — 예: `Northwind`를
      백업·복원한 `Northwind_Prod`. **감사 용도로 연결하면** 설계가 감사에도 병합을 연 것(`MappingPolicy`)까지
      함께 확인된다
- [ ] 티켓 브랜치를 개발 클론에서 준비해 Push한다
  - 서로 다른 객체를 고친 브랜치 둘 이상 (정상 병합, 테스트 반영 열)
  - 같은 `.sql`의 **같은 줄**을 다르게 고친 브랜치 둘 (충돌)
  - 같은 객체를 **다른 줄**에서 고친 브랜치 둘 (운영 클론의 "확인 필요")

### F-1. 테스트 클론 (`develop`)

- [ ] [병합할 브랜치 확인] → 목록에 `develop`·`master`가 없다
- [ ] 브랜치를 고르면 오른쪽에 바뀔 파일이 뜬다
- [ ] [병합] → 확인 → 원격에 병합 커밋이 보이고, DBVC 이력 탭 **종류**가 `병합`이다
- [ ] 병합 뒤 "차이 검사를 시작할까요?"에 확인 → Pull이 곧바로 끝나고 병합된 객체가 차이로 뜬다
- [ ] 같은 줄을 다르게 고친 브랜치 → 미리보기에 충돌이 뜨고 [병합]이 잠긴다
- [ ] 목록의 **테스트 반영** 열은 남아 있고 칸이 비어 있다

### F-2. 운영 클론 (`master`)

- [ ] **테스트 반영** 열이 `develop`에 병합된 브랜치에만 `반영됨`이다
- [ ] 같은 객체를 만진 브랜치 둘 중 하나를 고르면 미리보기에 **"확인 필요"** 가 뜨고, [병합]의 확인 대화상자에도 실린다

### F-3. 원격 경합과 실패

사람 둘이 필요한 상황은 개발 클론에서 원격에 직접 Push해 흉내 낸다.

- [ ] 미리보기 뒤 [병합] 전에 개발 클론에서 목적지 브랜치(`develop`)에 먼저 Push한다 → 오류·거부 안내가
      뜬 뒤 클론이 깨끗하다(`git status` 깨끗, 병합 전 커밋). [병합할 브랜치 확인]부터 다시 하면 병합된다
- [ ] 원격에서 브랜치 하나를 지운 뒤 [병합할 브랜치 확인] → 그 브랜치가 목록에서 사라진다
- [ ] 미리보기를 본 뒤 **그 원본 브랜치**에 새 커밋을 올리고 [병합] → "미리보기 뒤 … 새 커밋이
      올라왔습니다"로 거부되고, 원격의 목적지 브랜치가 그대로다

### F-4. 다른 화면과의 경계

- [ ] 개발 클론에서는 병합 영역이 보이지 않는다
- [ ] 병합 영역이 생긴 배포·감사 클론에서도 커밋·**Push** 버튼은 여전히 잠겨 있다 — 병합은 자기가 만든
      병합 커밋 하나만 올리고, Push 자체는 열지 않았다(`MappingPolicy`). D-1을 0.8.0에서 다시 보는 항목이다

---

## G. 도구 줄 정리 (0.9.0)

`b4aca10`으로 들어왔다. 변경 목록 도구 줄이 동기화·커밋·체크한 항목 세 줄로 묶이고, **전체 다시 추출**·
배포/롤백 스크립트·브랜치 조작이 메뉴 버튼(**새로고침 ▾**, **스크립트 ▾**, 브랜치 이름 **▾**) 안으로
들어갔다. 출처: `setup-checklist.md` "도구 줄 정리 (0.9.0)".

준비: 개발 클론 하나와 배포 클론 하나. 지금 환경이면 `Northwind`와 `Northwind_Deploy`로 된다.

### G-1. 테마와 좁은 폭

- [x] **밝은 테마와 어두운 테마 각각에서** 세 메뉴를 열어 글씨가 읽히고, 항목에 마우스를 올리면 강조가 보인다 — 2026-09-18
- [x] 도구 창을 좁게 도킹하면 **Pull·Push·원격 확인** 셋이 **함께** 다음 줄로 내려간다(하나만 떨어지지 않는다) — 2026-09-18
- [x] 좁은 폭에서 첫 줄의 "Windows 인증" 같은 대상 표시가 버튼에 가리지 않는다 — 2026-09-18

### G-2. 메뉴가 옛 버튼과 같은 일을 한다

- [x] **새로고침 ▾ → 전체 다시 추출** — 2026-09-18
- [x] **스크립트 ▾ → 배포 스크립트... / 롤백 스크립트...** — 2026-09-18
- [x] 브랜치 이름 **▾ → 브랜치 전환... / 새 브랜치...** — 2026-09-18

### G-3. 상태에 따라 잠기고 풀린다

- [x] **원격 확인** 뒤 Pull·Push 버튼에 `↓n`·`↑n`이 붙고, **Pull** 뒤와 다른 데이터베이스를 고른 뒤에는 사라진다 — 2026-09-18
- [x] 체크를 모두 풀면 **되돌리기**·**무시**가 바로 잠기고, **스크립트 ▾**는 열리되 두 항목이 잠겨 보인다 — 2026-09-18
- [x] "체크한 항목 n개"가 체크를 바꿀 때마다 따라온다 — 2026-09-18
- [x] 빈 커밋 메시지 칸에 회색 "커밋 메시지" 안내가 보이고, 글자를 치면 사라진다. 칸을 눌러 바로 입력할 수 있다 — 2026-09-18
- [x] **전체 다시 추출**이 도는 동안 세 메뉴 버튼이 잠긴다 — 2026-09-18

### G-4. 배포 클론

- [ ] 윗줄 브랜치 이름 **▾**를 열면 두 항목이 잠겨 보이고, 마우스를 올리면 고정 브랜치라 잠겼다는 안내가 뜬다
- [ ] 윗줄이 병합 영역 머리글(`병합 — 목적지: …`)을 가리거나 겹치지 않는다
- [ ] **병합할 브랜치 확인**이 도는 동안 윗줄 브랜치 버튼도 잠긴다
- [ ] Pull·Push·Commit 같은 개발 화면 버튼은 어디에도 보이지 않는다 (D-1을 0.9.0에서 다시 보는 항목이다)
- [ ] 병합 → "차이 검사 시작" 확인까지 한 바퀴가 0.8.0과 똑같이 돈다 — **F절을 그대로 한 번 더 따라간다**

---

## 뺀 항목과 이유

| 뺀 항목 | 이유 |
| --- | --- |
| 0.5.15 인코딩 전환 절차 전체(배너·버튼·취소·전환·바이트·GitLab diff·다른 PC Pull) | 옛 UTF-16 저장소를 가진 사람에게만 뜨는 1회성 경로다. 팀 배포 전이라 팀에는 그런 저장소가 없고, 개발 저장소는 2026-09-07에 이미 전환했으며(현재 BOM `EF BB BF`), diff가 텍스트로 나오는 것도 같은 날 확인했다. 남는 쓸모 있는 두 항목(한국어 깨짐, 배포 스크립트 BOM)은 B-4, D-2로 옮겼다 |
| 0.5.14 구버전 배너와 **추적기 업데이트** | 2026-09-09 v5 → v6 업그레이드에서 밟았다 |
| AI: `ai-settings.json`에 키 원문이 보이지 않는다 | 2026-09-16 "키 암호화" 실기 확인에 포함된다 |
| AI: 체크하지 않은 변경의 diff는 전송되지 않는다 | 실제 저장소로 두 파일 중 하나만 요청하는 자동 테스트(`117cafa`)가 지킨다. 수동으로는 모델 응답을 보고 추측할 수밖에 없어 그 테스트보다 약하다 |
| 브랜치: 고정 브랜치가 있는 **개발** 클론에서 전환 뒤 차단 오버레이 | 2026-09-09 정책 정정으로 개발 클론은 브랜치를 고정하지 않는다. 판정 로직은 `5269d41`의 ViewModel 테스트가 잠갔고, 오버레이가 그려지는지는 D-1이 본다 |
| 브랜치: 전환하면 원격 확인 숫자가 지워진다 | 속성 하나를 비우는 ViewModel 동작이라 `5269d41` 테스트가 지킨다. 같은 속성이 Pull·Push 뒤 지워지는 화면은 이미 쓰고 있다 |
| 이력 뷰 통합: 2단/3단 접힘, **전체 이력으로**, `병합` 종류 열과 안내 | 0.5.12·0.5.13에서 레이아웃과 안내 위치가 바뀌었고, C-1이 가리키는 절이 바뀐 동작 기준으로 더 자세히 본다 |
| 되돌리기: 배포 저장소에 되돌리기 버튼이 없다 | D-1 "변경 목록 패널이 없다"와 같은 확인이다 |

## 다 채운 뒤

알려 주면 결과를 `team-rollout-backlog.md` "남은 검증"과 해당 계획서·`setup-checklist.md`
체크박스에 옮기고, 실패 항목은 따로 추적한다.

## 이미 닫힌 것 (참고)

- 0.5.20 `.gitattributes` 3항목 — 2026-09-09
- 0.5.21 "무시"와 30일 보존(업그레이드 v5→v6, 신규 v6 설치) — 2026-09-09
- `.sql` diff가 텍스트로 보임 — 2026-09-07
- AI 옵션 페이지·키 암호화·생성 — 2026-09-16
